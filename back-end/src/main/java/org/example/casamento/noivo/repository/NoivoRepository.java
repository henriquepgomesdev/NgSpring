package org.example.casamento.noivo.repository;

import org.example.casamento.noivo.domain.Noivo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NoivoRepository extends JpaRepository<Noivo, Long> {
}
